name: yeidaraah_app
description: AI Food Rescue by Team Manna Hackers

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.32.0
  cloud_firestore: ^4.15.0
  firebase_auth: ^4.17.0
  google_maps_flutter: ^2.5.3
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  url_launcher: ^6.2.5
  geolocator: ^10.1.0
  google_generative_ai: ^0.2.0
  http: ^1.2.0
  intl: ^0.19.0
  provider: ^6.1.1
  image_picker: ^1.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true